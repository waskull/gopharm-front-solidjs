import { JSX } from "solid-js";

export default function NotFound(): JSX.Element {
    return (
        <div>
            <h1>La pagina solicitada no se ha encontrado :S</h1>
        </div>
    );
}